#include <iostream>

using namespace std;

int main(){
    freopen("censor.in", "r", stdin);
    freopen("censor.out", "w", stdout);

    str s;
    cin >> s;
    for(int i = 0; i<s.length(),++i){
        
    }
    }